import { PaginationItem } from '../paginationItem';
import { GetItemWidth } from './itemWidthCalculator';
export declare function createTotalWidthCalculator({ getItemWidth, outerFrameWidth, }: Params): (items: PaginationItem[]) => number;
declare type Params = {
    getItemWidth: GetItemWidth;
    outerFrameWidth: number;
};
export {};
