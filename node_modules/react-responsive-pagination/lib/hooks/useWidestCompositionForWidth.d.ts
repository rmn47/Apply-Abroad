import { ForwardedRef } from 'react';
import { PaginationItem } from '../paginationItem';
export declare function useWidestCompositionForWidth(narrowToWideCompositionsProvider: () => IterableIterator<PaginationItem[]>, maxWidth: number): {
    items: PaginationItem[];
    ref: ForwardedRef<HTMLElement | null>;
    clearCache: () => void;
};
