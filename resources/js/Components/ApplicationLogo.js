import React from 'react';

export default function ApplicationLogo({ className='' }) {
    return (
        <img 
        src="\logo\Flying_Bird.png" 
        className={className}
        />
    );
}
